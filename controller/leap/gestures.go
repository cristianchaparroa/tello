package leap

const (

	// GestureCircle is a single finger tracing a circle.
	GestureCircle = "circle"

	// GestureSwipe is a long, linear movement of a finger.
	GestureSwipe = "swipe"

	// GestureScreenTap is a tapping movement by the finger as if tapping a vertical computer screen.
	GestureScreenTap = "screenTap"

	// GestureKeyTap is tapping movement by a finger as if tapping a keyboard key.
	GestureKeyTap = "keyTap"
)
